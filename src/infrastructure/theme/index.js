import { colors } from "./colors.js";
import { space, lineHeights } from "./spacing.js";
import { sizes } from "./sizes.js";
import { fonts, fontWeights, fontSizes } from "./fonts.js";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};
